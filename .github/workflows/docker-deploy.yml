name: docker-deploy
run-name: ${{ github.actor }} is deploying with docker
on: [push]
jobs:
  build:
    runs-on: soldier
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
      -
        name: Docker Build
        run: docker build -t sensor-dashboard .
  deploy:
    runs-on: soldier
    steps:
      -
        name: deploy
        run: docker run sensor-dashboard